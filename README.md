# Архив метеослужбы

Показывает архив температуры и осадков в одном городе.

Запуск приложения:
```
npm i && npm run start
```

После этого приложение будет доступно на 8001 порту.


### Описание

Для осадков всегда выводится суммарные осадки за год.

Для температуры сделан адаптивный вывод:
 - если в график влезает температура по дням - рисуем по дням
 - если влезает график по неделям - рисуем по неделям
 - если влезает график по месяцам - рисуем  по месяцам
 - иначе рисуем среднегодовые температуры

Так как не получил вовремя ответа на свой вопрос по поводу группировки значений
(надо всегда выводить среднегодовые значения или сделать адаптивно),
то взял на себя инициативу и сделал адаптивно (как расписано выше).

С таким подходом возможна ситуация, когда пользователь хочет смотреть изменения среднегодовых температур
за небольшое число лет, но сделать этого не может, так как график адаптировался и выводит среднемесячные.
Решить это можно введением явного селектора для смены режима вывода
(среднедневных / средненедельных / среднемесячных / среднегодовых температур).

### ToDo:

Также как дальнейшее развитие проекта можно было бы сделать:
1. `service worker`, выводящий графики в режиме оффлайн (так как все данные могут уже быть в `indexedDB`, 
то `service worker` сюда идеально встанет)
2. вывод значения в тултипе при наведении мыши на график
3. больше обработок всяких нештатных ситуаций вроде браузеров, неподдерживающих `indexedDB`
или ошибок чтения баз
4. селектор группировки данных (среднедневные / средненедельные / среднемесячные / среднегодовые значения)
